import  dataStatusJson from "@data/dataStatus.json"
import  customerTypesJson from "@data/customerTypes.json"
import  chargingStatusListJson from "@data/customerTypes.json"

export const dataStatus = dataStatusJson;

export const customer_types = customerTypesJson;

export const charging_status_list = chargingStatusListJson;
