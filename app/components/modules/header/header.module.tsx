import React, { ReactComponentElement } from 'react';

import { Sty_Header } from './header.style';

type Props = {};

export const Header_Module:React.FC = (props: Props) => {
  return <Sty_Header>header</Sty_Header>;
};
